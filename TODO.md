# TODO — старт MVP SecMess

План составлен на основе текущего `TZ.md` и ориентирован на быстрый запуск первого рабочего контура.

## Этап 0. Базовый каркас проекта (P0)
- [x] Утвердить структуру репозитория: `synapse/`, `fluffychat/`, `keygen/`, `infra/`, `docs/`.
- [x] Создать базовый `docker-compose.yml` для `synapse + postgres + keygen + nginx`.
- [x] Создать `.env.example` с переменными для домена, БД, секретов Synapse и keygen.
- [x] Добавить `Makefile` или набор скриптов (`up`, `down`, `logs`, `init`) для локального запуска.
- [x] Зафиксировать минимальную схему сетей Docker: внешняя только для `nginx`, внутренние для сервисов.

## Этап 1. Synapse в закрытом контуре (P0)
- [x] Поднять Synapse на PostgreSQL (без sqlite).
- [x] Применить базовые security-настройки: `enable_registration: false`, закрытый федерационный контур.
- [x] Настроить rate limiting и базовые ограничения регистрации/логина.
- [x] Подготовить шаблон `homeserver.yaml` и вынести чувствительные параметры в env/секреты.
- [x] Проверить доступность через `nginx` + TLS (dev self-signed).

## Этап 2. Сервис `keygen` (QR-backend) (P0)
- [x] Инициализировать `keygen` как отдельный сервис (API + Dockerfile + зависимости).
- [x] Реализовать модель одноразового токена: `token`, `ttl`, `created_by`, `used_at`, `revoked_at`.
- [x] Реализовать endpoint генерации токена (только для администратора).
- [x] Реализовать endpoint валидации/погашения токена (single-use, атомарно).
- [x] Интегрировать вызов Synapse Admin API для создания пользователя и выдачи `access_token`.
- [x] Добавить генерацию QR (PNG/SVG) для токена.

## Этап 3. Android-клиент FluffyChat (P0)
- [x] Зафиксировать Android-only dev цикл (эмулятор, debug, сборка apk).
- [x] Зашить адрес homeserver в конфиг клиента для MVP.
- [x] Убрать/скрыть лишние функции из scope MVP (Spaces, публичный поиск и т.д.).
- [x] Добавить экран/поток входа через QR-сканирование.
- [x] Подключить клиент к `keygen` API: обмен QR-токена на `access_token`.
- [x] Проверить первый вход и инициализацию E2EE-ключей на устройстве. _(ручной тест на устройстве)_

## Этап 4. Безопасность и эксплуатация (P1)
- [x] Подготовить базовый hardening-гайд для VPS: UFW, SSH, Fail2Ban.
- [x] Настроить ротацию логов и сбор логов ошибок Synapse/keygen/nginx.
- [x] Подготовить стратегию резервного копирования: Postgres + конфиги + медиа.
- [x] Описать процедуру восстановления (backup/restore drill).

## Этап 5. Мастер-ключ и админ-контур (P1)
- [x] Спроектировать формат мастер-ключа и правила хранения/передачи офлайн.
- [x] Реализовать проверку мастер-ключа в админ-endpoint'ах `keygen`.
- [x] Реализовать ротацию мастер-ключа с инвалидированием старого.
- [x] Добавить аудит-события для админ-действий (кто, когда, что сделал).

## Этап 6. Документация и сдача артефактов (P0)
- [x] `docs/deploy.md` — установка на чистый Ubuntu через Docker.
- [x] `docs/admin.md` — генерация QR, управление доступом, ротация мастер-ключа.
- [x] `docs/client-build.md` — смена адреса сервера и пересборка APK.
- [x] `docs/backup-restore.md` — аварийное восстановление.
- [x] Финальная проверка: поднятие системы по документации с нуля.

## Ближайшие шаги (с чего начать прямо сейчас)
- [x] 1. Создать `infra/docker-compose.yml` (Synapse + Postgres + Nginx + заглушка keygen).
- [x] 2. Добавить `infra/.env.example` и `infra/nginx/nginx.conf`.
- [x] 3. Подготовить рабочий `homeserver.yaml` шаблон под закрытый сервер без федерации.
- [x] 4. Инициализировать `keygen` сервис (скелет API + healthcheck + Dockerfile).
- [x] 5. Описать и зафиксировать API-контракт для QR-логина (`/token/create`, `/token/redeem`).

